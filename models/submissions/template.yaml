# ABSOLUTE MINIMUM SUBMISSION FORMAT
# Only ONE field is required!

model:
  hf_repo: "username/model-name"  # <--- REPLACE THIS 
  
# That's it! 
# We will automatically detect:
# - Model Name
# - Architecture
# - Parameters
# - Context Length
# - Available Quantizations

# ------------------------------------------------------------------
# OPTIONAL OVERRIDES (Only use if needed)
# ------------------------------------------------------------------
# model:
#   hf_repo: "username/model-name"
#   name: "My Custom Display Name"
#   categories: ["reasoning", "coding"]  # Restrict to specific benchmarks
#   skip_quantization: ["gguf"]
#   quick_test: true 
